<!DOCTYPE html>
<html lang="fr">
	<head>
		<meta charset="UTF-8">
		<style>
			@import url('https://fonts.googleapis.com/css2?family=Playfair+Display:wght@500;600;700&family=DM+Sans:wght@400;500;600;700&display=swap');

			* {
				box-sizing: border-box;
				margin: 0;
				padding: 0;
			}

			body {
				font-family: 'DM Sans', 'DejaVu Sans', sans-serif;
				font-size: 12px;
				color: #211a10;
				background: #fff;
				margin: 0;
				padding: 40px 50px;
				line-height: 1.6;
			}

			/* ── HEADER ─────────────────────────────────────── */
			.header {
				border-bottom: 3px solid #211a10;
				padding-bottom: 24px;
				margin-bottom: 32px;
				position: relative;
			}

			.header-inner {
				width: 100%;
				overflow: hidden;
			}

			.brand {
				float: left;
				width: 55%;
			}

			.brand-name {
				font-family: 'Playfair Display', serif;
				font-size: 26px;
				font-weight: 700;
				letter-spacing: 1px;
				color: #211a10;
				line-height: 1;
				margin-bottom: 8px;
			}

			.brand-tagline {
				font-size: 9px;
				letter-spacing: 4px;
				text-transform: uppercase;
				color: #b9852f;
				font-weight: 600;
				margin-bottom: 10px;
			}

			.brand-address {
				font-size: 10px;
				color: #8a7c62;
				line-height: 1.7;
			}

			.invoice-meta {
				float: right;
				width: 40%;
				text-align: right;
			}

			.invoice-label {
				font-family: 'Playfair Display', serif;
				font-size: 30px;
				font-weight: 600;
				letter-spacing: 4px;
				text-transform: uppercase;
				color: #211a10;
				line-height: 1;
				margin-bottom: 12px;
			}

			.invoice-number {
				display: inline-block;
				background: #211a10;
				color: #d9ac5c;
				font-size: 11px;
				font-weight: 600;
				letter-spacing: 2px;
				padding: 4px 12px;
				margin-bottom: 6px;
			}

			.invoice-date {
				font-size: 10px;
				color: #8a7c62;
				letter-spacing: 1px;
			}

			.clear {
				clear: both;
			}

			/* ── ACCENT BAR ──────────────────────────────────── */
			.accent-bar {
				height: 4px;
				background: linear-gradient(to right, #b9852f 0%, #d9ac5c 50%, #b9852f 100%);
				margin-bottom: 32px;
			}

			/* ── INFO GRID ───────────────────────────────────── */
			.info-grid {
				width: 100%;
				overflow: hidden;
				margin-bottom: 32px;
			}

			.info-box {
				float: left;
				width: 48%;
				border: 1px solid #ece2cb;
				border-top: 3px solid #b9852f;
				padding: 16px 18px;
				background: #fbf6ec;
			}

			.info-box + .info-box {
				float: right;
			}

			.info-box-title {
				font-size: 8px;
				font-weight: 700;
				letter-spacing: 3px;
				text-transform: uppercase;
				color: #b9852f;
				margin-bottom: 8px;
			}

			.info-box-content {
				font-size: 11px;
				color: #3c3120;
				line-height: 1.8;
			}

			.info-box-content strong {
				font-size: 12px;
				color: #211a10;
				font-weight: 600;
			}

			/* ── TABLE ───────────────────────────────────────── */
			.section-label {
				font-size: 8px;
				font-weight: 700;
				letter-spacing: 3px;
				text-transform: uppercase;
				color: #8a7c62;
				margin-bottom: 10px;
				padding-left: 2px;
			}

			table {
				width: 100%;
				border-collapse: collapse;
				margin-bottom: 28px;
			}

			table thead tr {
				background: #211a10;
			}

			table thead th {
				padding: 11px 14px;
				color: #d9ac5c;
				font-size: 9px;
				font-weight: 700;
				letter-spacing: 2px;
				text-transform: uppercase;
				text-align: left;
				border: none;
			}

			table thead th.text-right {
				text-align: right;
			}

			table tbody tr {
				border-bottom: 1px solid #ece2cb;
			}

			table tbody tr:nth-child(even) {
				background: #fbf6ec;
			}

			table tbody td {
				padding: 11px 14px;
				font-size: 11px;
				color: #3c3120;
				border: none;
			}

			.product-name {
				font-weight: 600;
				color: #211a10;
			}

			.tva-badge {
				display: inline-block;
				background: #f4ead4;
				color: #9c6f26;
				font-size: 9px;
				font-weight: 700;
				padding: 2px 7px;
				border-radius: 10px;
			}

			.text-right {
				text-align: right;
			}
			.text-center {
				text-align: center;
			}

			/* ── LIVRAISON ───────────────────────────────────── */
			.shipping-row {
				width: 100%;
				overflow: hidden;
				padding: 10px 14px;
				background: #fbf6ec;
				border-left: 3px solid #b9852f;
				margin-bottom: 28px;
				font-size: 11px;
				color: #3c3120;
			}

			.shipping-row span {
				font-weight: 600;
				color: #211a10;
			}

			/* ── TOTAUX ──────────────────────────────────────── */
			.totals-wrap {
				width: 320px;
				margin-left: auto;
			}

			.totals-table {
				width: 100%;
				border-collapse: collapse;
				margin-bottom: 0;
			}

			.totals-table td {
				padding: 8px 14px;
				font-size: 11px;
				border: none;
				color: #3c3120;
			}

			.totals-table tr.tva-row td {
				background: #fbf6ec;
				border-top: 1px solid #ece2cb;
				border-bottom: 1px solid #ece2cb;
			}

			.totals-table tr.total-row {
				background: #211a10;
			}

			.totals-table tr.total-row td {
				color: #fff;
				font-size: 13px;
				font-weight: 700;
				padding: 13px 14px;
				letter-spacing: 1px;
			}

			.totals-table tr.total-row td.amount {
				color: #d9ac5c;
				font-size: 15px;
				text-align: right;
			}

			/* ── FOOTER ──────────────────────────────────────── */
			.footer {
				margin-top: 48px;
				border-top: 1px solid #ece2cb;
				padding-top: 16px;
				text-align: center;
			}

			.footer-thanks {
				font-family: 'Playfair Display', serif;
				font-size: 14px;
				font-weight: 600;
				letter-spacing: 2px;
				text-transform: uppercase;
				color: #211a10;
				margin-bottom: 6px;
			}

			.footer-note {
				font-size: 9px;
				color: #8a7c62;
				letter-spacing: 1px;
			}

			.gold {
				color: #b9852f;
			}
		</style>
	</head>
	<body>

		<!-- HEADER -->
		<div class="header">
			<div class="header-inner">
				<div class="brand">
					{% if logoBase64 %}
						<img src="{{ logoBase64 }}" alt="Logo de la boutique" width="120" height="auto">
					{% else %}
						<div class="brand-name">Dubai Dream</div>
					{% endif %}
					<div class="brand-tagline">Luxe Oriental</div>
					<div class="brand-address">
						68 boulevard des couteaux &bull; 59150 Wattrelos<br>
						contact@dubaidream.fr
					</div>
				</div>
				<div class="invoice-meta">
					<div class="invoice-label">Facture</div>
					<div class="invoice-number">N°
						{{ order.id }}</div>
					<div class="invoice-date">{{ order.createdAt|date('d/m/Y') }}</div>
				</div>
				<div class="clear"></div>
			</div>
		</div>

		<div class="accent-bar"></div>

		<!-- CLIENT + LIVRAISON -->
		<div class="info-grid">
			<div class="info-box">
				<div class="info-box-title">Client</div>
				<div class="info-box-content">
					<strong>{{ order.user.firstname }}
						{{ order.user.lastname }}</strong><br>
					{{ order.user.email }}
				</div>
			</div>
			<div class="info-box">
				<div class="info-box-title">Adresse de livraison</div>
				<div class="info-box-content" style="white-space:pre-line;">{{ order.delivery|raw }}</div>
			</div>
			<div class="clear"></div>
		</div>

		<!-- PRODUITS -->
		<div class="section-label">Détail de la commande</div>
		<table>
			<thead>
				<tr>
					<th>Produit</th>
					<th class="text-center">TVA</th>
					<th class="text-center">Qté</th>
					<th class="text-right">Prix TTC</th>
					<th class="text-right">Total</th>
				</tr>
			</thead>
			<tbody>
				{% for product in order.orderDetails %}
					<tr>
						<td class="product-name">{{ product.productName }}</td>
						<td class="text-center">
							<span class="tva-badge">{{ product.productTva }}%</span>
						</td>
						<td class="text-center">{{ product.productQuantity }}</td>
						<td class="text-right">{{ product.getProductPriceWt()|number_format(2,',','.') }}
							€</td>
						<td class="text-right">{{ (product.getProductPriceWt() * product.productQuantity)|number_format(2,',','.') }}
							€</td>
					</tr>
				{% endfor %}
			</tbody>
		</table>

		<!-- LIVRAISON -->
		<div class="shipping-row">
			Livraison &mdash;
			<span>{{ order.carrierName }}</span>
			&nbsp;&nbsp;&bull;&nbsp;&nbsp;
			{{ order.carrierPrice|number_format(2,',','.') }}
			€
		</div>

		<!-- TOTAUX -->
		<div class="totals-wrap">
			<table class="totals-table">
				<tr class="tva-row">
					<td>TVA totale</td>
					<td class="text-right">{{ order.getTotalTva()|number_format(2,',','.') }}
						€</td>
				</tr>
				<tr class="total-row">
					<td>TOTAL TTC</td>
					<td class="amount">{{ order.getTotalWt()|number_format(2,',','.') }}
						€</td>
				</tr>
			</table>
		</div>

		<!-- FOOTER -->
		<div class="footer">
			<div class="footer-thanks">Merci pour votre commande
				<span class="gold">&hearts;</span>
			</div>
			<div class="footer-note">Ce document tient lieu de facture &bull; Dubai Dream &mdash; 59150 Wattrelos</div>
		</div>

	</body>
</html>